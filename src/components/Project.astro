---

const {
  title,
  thumbnail,
  content,
  background = '#E4E4E4',
  color = '#fff',
  iconWithoutIndent = false,
  alignTop = false,
  small,
} = Astro.props;

---

<div class={`project ${small ? 'project__mini' : ''} ${alignTop ? 'project__align-top' : ''}`} title={title} style={`color: ${color}; background-color: ${background}`}>
	<img src={thumbnail} alt="" class={`project_image ${iconWithoutIndent ? 'project_image__without-indent' : ''}`}>

  {small && <button class="project_more">explore <span>case</span></button>}

  {!small && (
    <div class="project_content">
      <Fragment set:html={content}/>
    </div>
  )}
</div>
